from setuptools import setup

setup(
    name='CryptonalysisServer',
    version='0.1',
    packages=['cryptonalysis'],
    include_package_data=True,
    install_requires=[
        'flask',
    ],
)
